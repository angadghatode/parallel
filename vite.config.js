import { defineConfig } from 'vite';

export default defineConfig({
  // REPLACE 'my-parallel' with your exact repository name
  base: '/angadghatode.github.io/parallel/', 
  build: {
    outDir: 'dist',
  }
});